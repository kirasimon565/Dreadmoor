version: 2

updates:
  - package-ecosystem: "pub"
    directory: "/"
    schedule:
      interval: "weekly"

    open-pull-requests-limit: 1

    commit-message:
      prefix: "deps"

    labels:
      - "dependencies"

    groups:
      flutter-dependencies:
        patterns:
          - "*"
